# segredos — NUNCA subir
local_config.py

# dados privados (contêm email) — nunca subir
data/scouts_raw.json

__pycache__/
*.pyc
.DS_Store

# diagnósticos temporários
data/_*.json

# cookie de sessão Apollo — NUNCA subir
apollo_cookies.json
